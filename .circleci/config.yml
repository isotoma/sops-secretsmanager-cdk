version: 2

jobs:
  publish:
    working_directory: ~/repo
    docker:
      - image: circleci/node:8.9.1
    steps:
      - checkout

      - run: npm install

      - run: ls -al

workflows:
  version: 2
  publish:
    jobs:
      - publish:
          filters:
            tags:
              only: /\d+\.\d+\.\d+/
            branches:
              only: ci
              # ignore: /.*/
